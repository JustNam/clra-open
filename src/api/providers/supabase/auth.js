// Supabase implementation of the auth API — STUB.
// When implemented, signup/login should return { token, user } (token can be
// the Supabase access_token) and me() should return the current user, so the
// shared AuthProvider strategy keeps working unchanged.
import { notImplemented } from './_notImplemented'

export const AuthApi = {
  signup: notImplemented('AuthApi.signup'),
  login: notImplemented('AuthApi.login'),
  me: notImplemented('AuthApi.me'),
}
