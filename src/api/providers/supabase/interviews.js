// Supabase implementation of the interviews API — STUB.
// Same method signatures and return shapes as the valtown provider; reuse
// interviewAdapter when filling these in (Supabase rows are snake_case too).
import { notImplemented } from './_notImplemented'

export const InterviewsApi = {
  list: notImplemented('InterviewsApi.list'),
  getById: notImplemented('InterviewsApi.getById'),
  create: notImplemented('InterviewsApi.create'),
  update: notImplemented('InterviewsApi.update'),
  remove: notImplemented('InterviewsApi.remove'),
}
