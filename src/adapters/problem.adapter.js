// TODO (student): implement toFrontend, listToFrontend, toDatabase
// following the same pattern as interview.adapter.js
export const problemAdapter = {
  toFrontend: (raw) => raw,
  listToFrontend: (rows) => rows,
  toDatabase: (data) => data,
}
